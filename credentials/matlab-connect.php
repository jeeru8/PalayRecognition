<?php 
	$servername = "localhost";
	$username = "root";
	$password = "";
	$db = "prsystem_db";

	// Create connection
	$conn = mysqli_connect($servername, $username, $password, $db);

	// Check connection
	if (!$conn) {
	    die("Connection failed: " . mysqli_connect_error());
	}


	if_server_load = {connect_image_train_to_execute}
	files_to_load_module_array = ( /MATLAB IMAGE PROCESSING SCRIPTS )...
	
	execute();
	connect();
	load();
	
?>